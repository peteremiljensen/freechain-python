notifications:
  email: false
language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install coveralls
  - pip install -r python/requirements.txt
script:
  - cd python && python -m unittest blockchain.test_unit blockchain.test_integration
  - coverage run --source=blockchain --omit="blockchain/test_*" -m unittest blockchain.test_unit blockchain.test_integration
after_success:
  coveralls
