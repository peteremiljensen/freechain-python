notifications:
  email: false
language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install coveralls
  - pip install -r requirements.txt
script:
  - coverage run --omit="test_*" -m unittest test_unit test_integration
after_success:
  coveralls
